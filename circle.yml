#
# Configuration for https://circleci.com/gh/mjuenema/python-TSIP
#

machine:
  python:
    version: 2.6.6 2.6.8 2.7.3

dependencies:
  pre:
    - pip install -r requirements.txt
    - pip install coverage

test:
  override:
    - make test
