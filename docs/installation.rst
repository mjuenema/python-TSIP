============
Installation
============

At the command line::

    $ easy_install python-TSIP

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv python-TSIP
    $ pip install python-TSIP
